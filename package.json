{
  "name": "mcp-tmpfs-server",
  "version": "1.0.2",
  "description": "MCP server that lists, reads, writes, and unzips files in a sandboxed temp directory.",
  "bin": {
    "mcp-tmpfs-server": "./index.js"
  },
  "main": "index.js",
  "files": [
    "index.js"
  ],
  "scripts": {
    "test": "node test.js",
    "lint": "./dev-package/node_modules/.bin/eslint .",
    "prepare": "./dev-package/node_modules/.bin/husky install"
  },
  "engines": {
    "node": ">=18"
  }
}
